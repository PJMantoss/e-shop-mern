import StripeCheckout from 'react-stripe-checkout';

const Pay = () => {
    return(
        <div 
            style={{
                height:
                display:
                alignItems:
                justifyContent:
            }}
        >
            <button></button>
        </div>
    );
}