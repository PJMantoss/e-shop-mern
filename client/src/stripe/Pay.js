import StripeCheckout from 'react-stripe-checkout';

const Pay = () => {
    return(
        <div 
            style={{
                height: "100vh",
                display: "flex",
                alignItems: "center",
                justifyContent: "center"
            }}
        >
            <button
                style={{
                    border: "100vh",
                    width: "flex",
                    borderRadius: "center",
                    padding: "center"
                }}
            ></button>
        </div>
    );
}